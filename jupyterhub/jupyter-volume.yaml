apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: jupyter-volume-username
spec:
  storageClassName: rook-ceph-block-tide
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi